user:
  email:
    max_length: 255
  email_regex: !ruby/regexp '/\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i'
  name:
    max_length: 50
  password:
    min_length: 6
users_controller:
  size:
    per_page: 10
users_helper:
  size:
    size_length: 80
users:
  size:
    max_length: 50
edit:
  link_href: "https://gravatar.com/emails"
