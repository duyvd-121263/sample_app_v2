<%= form_for(find_user_active(current_user, @user),
  html: {method: :delete}, remote: true) do |f| %>
  <%= f.submit t ".unfollow", class: "btn" %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
<% end %>
